# 실증 키오스크 앱 개발 플랜 — 모리 IP × 정서·창의 2트랙 × limited 영상 파이프라인

> 작성일 2026-07-02 · 목적: 도서관 좌식 키오스크(갤럭시탭 + Fully Kiosk Browser)에서 돌아갈 "AI 책 놀이" 데모 앱의 화면·콘텐츠·기술 개발 플랜
> 합의된 전제: ① 콘텐츠 축 = **모리 가이드 + 정서·창의 2트랙** (과목형 아님) ② 샘플 영상 = **limited 모드 중심**(오프닝 1~2편만 premium) ③ 이번 산출물 = 플랜 문서 (구현은 다음 세션)
> 연계 문서: [`KIOSK_하드웨어_제작계획.md`](./KIOSK_하드웨어_제작계획.md)(v2, 바우처 규정) · [`KIOSK_KINDY_퍼널추적_설계.md`](./KIOSK_KINDY_퍼널추적_설계.md) · [`AI책놀이_데모_UIUX기획.md`](./AI책놀이_데모_UIUX기획.md) · [`IP_북메이트_캐릭터바이블.md`](./IP_북메이트_캐릭터바이블.md) · [`docs/VIDEO_LIMITED_ANIMATION_PIPELINE.md`](./docs/VIDEO_LIMITED_ANIMATION_PIPELINE.md) · [`방향전환_정서창의성_핸드오프.md`](./방향전환_정서창의성_핸드오프.md)

## 한 줄

> **도서관에서 아이가 "오늘 기분"과 "이야기 씨앗"을 고르면, 모리가 60~90초 정서·창의 영상과 마음 퀴즈로 놀아주고, 부모에게 "집에서 모리와 계속" QR을 건넨다.**

---

## 1. 현재 코드 델타 — 생각보다 많이 와 있다

`/demo/kiosk`(466줄)는 이미 구형 과목 데모가 아니라 **신방향으로 절반 전환된 상태**다.

| 영역 | 현재 상태 | 남은 것 |
|---|---|---|
| 선택지 | ✅ 모리·꾸미·방울·나옹 4친구 + 창의스킬 4종(자세히보기/닮은것잇기/규칙찾기/모아서만들기) + 기분 4종 | 3문항 → **2문항 재설계**(§3), 선택지 의미 재정의 |
| 캐릭터 비주얼 | ⚠️ 인라인 placeholder SVG (공주/공룡 재활용) + `MoriCharacter.tsx`(38줄) | **R3 IP 에셋으로 교체** — `public/ip/generated/`(mori-hero, mori-cutout, 씨앗 4종) 활용 + 정서톤 4변형 생성 |
| 샘플 영상 | ⚠️ `mori-starlight-seed.mp4` 1편 고정 (`pickDemoVideo()` TODO 주석) | **씨앗×기분 매칭 세트 제작**(§4) + 매칭 로직 |
| 퍼널 이벤트 | ✅ 세션 생성, demo_started/step_select/video_*/qr_shown, `locationCode` 전송 | locationCode를 상수 → **URL `?loc=` 파라미터**로 |
| QR 어트리뷰션 | ✅ 실 QRCode + `/start?ks=` + 쿠키 + parent_attribution | 결과 화면 카피를 모리 톤으로 |
| 도서관 환경 | ✅ muted + 자막 상시 ON | — |
| 퀴즈(S4) | ❌ 없음 (영상→QR 직행) | **키오스크 전용 경량 퀴즈** 신규 (§3 S4) |
| 무인 운영 | ❌ idle 리셋·하드닝·오프라인 캐싱 없음 | §5 전부 신규 |

> 재사용 원칙(방향전환 핸드오프와 동일): 기존 퍼널 스키마(0015)·이벤트 API·어트리뷰션은 **그대로 위에 얹는다**. 가정용 20분 루프(SessionShell 등 11패키지)는 키오스크에 **이식하지 않는다** — 회전율 90초 환경에 맞는 경량 전용 컴포넌트를 만든다.

---

## 2. 경험 설계 — "모리가 처음부터 끝까지 안내한다"

IP 바이블의 다음 단계 ③("키오스크 데모에 가이드로 삽입 — 인트로·기분선택·퀴즈·결과")을 그대로 실행한다. 모리는 화면 구석 장식이 아니라 **매 화면의 화자**다.

### 플로우 (총 60~90초 + 퀴즈 ~30초, 질문 2개로 압축)

```
S0 인트로 ──▶ S1 오늘 기분 ──▶ S2 이야기 씨앗 ──▶ S3 영상(60~90s) ──▶ S4 마음 퀴즈(2문항) ──▶ S5 결과+QR
   │                                                                                        │
   └────────────────────────── idle 30초 → 홈 복귀 + 세션 종료 마킹 ←──────────────────────────┘
```

- **S0 인트로**: 모리(R3 히어로)가 인사 — "안녕! 난 모리야. 오늘 너랑 이야기를 만들고 싶어." + `시작하기` 큰 버튼. 상단에 도서관명(신뢰 차용).
- **S1 오늘 기분** (질문 1/2 · 정서 트랙 입력): IP 바이블의 **4 정서톤이 그대로 선택지** — 차분(하늘민트)/신나는(노랑코랄)/모험(보라별)/포근(분홍복숭아). 선택지 = 모리 정서톤 변형 이미지 4장. 선택하면 이후 모든 화면의 하트빛·배경톤이 그 정서톤으로 물든다(캐릭터 본체는 고정 — 바이블 규칙). `step_select(payload: mood)`.
- **S2 이야기 씨앗** (질문 2/2 · 창의 트랙 입력): 기존 생성 에셋 재사용 — **별빛 씨앗 / 숨은 숲 / 축제 방울 / 선물 상자** (`public/ip/generated/*.png`). "어떤 씨앗으로 이야기를 키울까?" `step_select(payload: seed)`. 선택 0.7초 후 자동 전환(기존 패턴 유지).
  - 현재 코드의 3문항(친구/스킬/기분) 중 '친구' 선택은 제거 — **모리 단일 가이드**(IP 통합 원칙: 미리 등은 영상 속 조연). 창의스킬 4종은 선택지가 아니라 **씨앗별 영상에 내장**된 학습 목표로 이동.
- **S3 영상**: 씨앗×기분 매칭 샘플 재생(§4). muted+자막 유지. 스킵 버튼 작게(스킵도 데이터). `video_started/progress/completed`.
- **S4 마음 퀴즈** (신규 · 2문항 ~30초):
  - Q1 정서(CASEL 감정인식): "모리 마음이 어땠을까?" — 표정 3택, 영상 장면 스틸 + 모리 표정 에셋. 정답 있되 오답도 "그럴 수도 있겠다! 같이 다시 볼까?" 톤(바이블: 정답 강요 X).
  - Q2 창의(Torrance 참여도): "씨앗에서 또 뭐가 자랄 수 있을까?" — 그림 3택, **정답 없음**, 무엇을 골라도 축하. 창의 트랙 철학(참여도=점수)의 키오스크 축소판.
  - `quiz_started/completed(score)/skipped`. 별 스티커 획득 연출(획득 별이 S5로 이어짐).
- **S5 결과 + QR**: 상단(아이) — 모리가 별을 들고 축하 + 정서톤 배경. 하단(부모, 차분한 카드) — "**오늘 만든 이야기의 다음 화**를 집에서 모리와 만들 수 있어요" + QR(`/start?ks=`). 연속 서사 제품 방향과 카피가 일치해야 함: 키오스크에서 심은 씨앗(선택값)이 가입 후 온보딩에 프리필되어 "이어짐"을 실감시킨다(kiosk_sessions.mood/seed → 온보딩 초기값, 후속 개발).
- **Idle/Reset**: 전 단계 무동작 30초 → S0 복귀, 세션 last_event 마킹. 영상 재생 중은 예외(영상 길이 + 10초).

### 카피 톤

모리 보이스 규칙(바이블) 준수: 친근한 반말, 짧고 따뜻, 정답 강요 금지. 브랜드명 미노출("AI 책 놀이") — 부모 카드도 혜택 중심.

---

## 3. 콘텐츠 제작 플랜 — limited 파이프라인으로 "거의 0원" 세트

### 영상 매트릭스 (MVP → V1 2단계)

| 단계 | 구성 | 편수 | 제작 방식 |
|---|---|---|---|
| **MVP (설치 시점)** | 씨앗 4종 × 대표 1편 (60~90초). 기분은 영상 분기 대신 **모리 멘트·화면 톤**으로 반영 | **4편** | limited 모드 (키프레임+TTS+ffmpeg) |
| 오프닝 | S0 배경용 모리 인사 루프(5~8초, 무음) | 1편 | **premium** (Seedance) — 첫인상 컷에만 비용 사용 (파이프라인 문서의 모드 구분 원칙 그대로) |
| **V1 (파일럿 2~3주차)** | 완주율 데이터 보고 상위 씨앗부터 기분 분기 확장 (4씨앗×4기분) | +12편 | limited |

- MVP를 4편으로 묶는 근거: ① limited라도 편당 키프레임 6~10컷 + TTS 제작·QA 시간이 든다 ② 기분 선택의 체감은 **UI 톤 변화 + 모리 멘트**로 먼저 검증 가능 ③ 16편을 미리 만들면 파일럿 데이터 없이 콘텐츠에 과투자.
- 각 편의 구조(파이프라인 제작 가이드 준수): 씬당 한 행동, 말하는 씬은 정면 medium close-up(입뻐끔), 나레이션 씬은 오브젝트 중심 pan. **정서 비트 1개 + 창의 스킬 1개**를 60~90초 안에: 예) '별빛 씨앗' = 씨앗이 자라며(창의: 자세히 보기) 모리가 설렘→차분을 오가는(정서: 감정 알아채기) 미니 서사.
- 기존 `generate-library-episode-90s.ts`(limited 모드)를 **키오스크 프리셋으로 파생**: 9:16 세로 출력(현 파이프라인은 16:9 — 세로 크롭 or 세로 키프레임 생성 결정 필요, §7 리스크), 자막 vtt 필수, muted 전제라 TTS는 자막 싱크용.
- **캐릭터 일관성**: `gen-ip.ts` 방식대로 R3 히어로를 refs로 고정. 키오스크 등장 전 IP 바이블 다음단계 ①(표정 시트 6종)·②(2D 셀 스타일 파생) 선행 — 영상·퀴즈 표정 선택지에 공용.

### 정지 에셋 (영상 외)

| 에셋 | 용도 | 상태 |
|---|---|---|
| 모리 정서톤 4변형 (`bookmate-ip-m_*.png` 체계) | S1 선택지, 단계별 배경톤 | ❌ 생성 필요 (`gen-ip.ts` + R3 refs) |
| 씨앗 4종 | S2 선택지 | ✅ `public/ip/generated/` 존재 |
| 모리 표정 시트 (기쁨/슬픔/놀람/설렘/차분/뿌듯) | S4 퀴즈 선택지, 정오답 리액션 | ❌ 생성 필요 |
| 별 스티커·축하 연출 | S4~S5 | ❌ 소품 (gift-box·balloon 등 기존 에셋 일부 재활용 가능) |

### 퀴즈 콘텐츠

편당 정서 1문항 + 창의 1문항 = MVP 8문항. LLM 실시간 생성 없이 **영상 제작 시 함께 저작해 JSON 정적 번들**(핫패스 LLM 0 원칙 유지). 스키마: `{ videoId, questions: [{ type: 'emotion'|'creative', prompt, choices: [{img, label, correct?}], reaction: {...} }] }`.

---

## 4. 기술 아키텍처

### 라우팅·구성

- **같은 kindy-web(Next.js)에 유지** — 퍼널 단일 저장소 원칙(설계 문서 결정 ②). `/demo/kiosk` → **`/kiosk`로 승격** + `robots noindex` + 전용 layout(네비 없음, 세이프존, 커서 숨김).
- 기기 구성은 전부 URL로: `/kiosk?loc=asan-kkumsaem` — `loc`을 세션 생성 payload의 `location_code`로 (현재 상수 하드코딩 → 파라미터화). Fully Kiosk 시작 URL만 바꾸면 다도서관 배포 끝.

### 무인 운영 하드닝 (P0)

- **idle 타이머**: 전역 포인터 이벤트 리스너 + 단계별 타임아웃(§2). 리셋 시 상태·세션 정리.
- **입력 차단**: `touch-action: manipulation`, 롱프레스 컨텍스트메뉴·텍스트 선택·핀치줌 CSS/JS 차단, 드래그 방지.
- **생존성**: 일 1회 새벽 자동 `location.reload()`(메모리 누수 방어), 전역 error boundary → 5초 후 자동 홈 복귀(에러 화면을 아이에게 노출하지 않음), `navigator.onLine` 감시.
- **화면**: Fully Kiosk가 wake lock·부팅 자동실행·원격 모니터링 담당(하드웨어 계획 §2). 앱은 PWA manifest(전체화면)만 제공.

### 오프라인 내성 (P0 — 도서관 Wi-Fi 품질을 신뢰하지 않는다)

- **미디어 프리로드**: 영상 4편(각 60~90초, limited라 비트레이트 낮음 — 편당 ~10-20MB) + 에셋 전부를 Service Worker cache-first로 프리캐시. 첫 로드 후 재생은 네트워크 무관.
- **이벤트 아웃박스**: `kioskTrack`을 큐 기반으로 — localStorage 아웃박스에 적재 → 온라인 시 배치 flush. QR 토큰은 세션 생성 시 서버 발급이므로, **오프라인 세션은 로컬 UUID로 진행하고 복구 시 세션+이벤트 일괄 업로드**(qr_token만은 서버 필수 → 오프라인 시 QR 카드에 "일반 시작 링크 + loc 파라미터" 폴백, 어트리뷰션 정밀도만 낮아지고 기능은 유지).

### 성능 목표

탭 보급형(50만 미만) 기준: 첫 진입 TTI < 3초(프리캐시 후 < 1초), 영상 시작 < 0.5초(캐시), 터치 반응 < 100ms. 애니메이션은 transform/opacity만.

---

## 5. 개발 백로그 (Phase별)

### Phase 0 — 에셋 선행 (개발과 병렬, ~3일)
1. 모리 정서톤 4변형 + 표정 시트 6종 생성 (`gen-ip.ts`, R3 refs 고정)
2. limited 파이프라인 키오스크 프리셋 결정(세로 대응) + 샘플 1편 파일럿 렌더 → 품질 확인 후 4편 양산
3. 퀴즈 8문항 저작(JSON) + 오프닝 premium 1편

### Phase 1 — 키오스크 MVP (설치 필수, ~1주)
4. `/kiosk` 라우트 승격 + 전용 layout + `?loc=` 파라미터화
5. 플로우 재설계: 3문항→2문항(기분·씨앗), 친구 선택 제거, placeholder SVG → IP 에셋 교체, 정서톤 테마 적용
6. 영상 매칭 로직(`pickDemoVideo` → 씨앗 기반 + 기분 멘트 분기)
7. S4 마음 퀴즈 컴포넌트(경량 신규) + quiz 이벤트 배선
8. S5 결과 화면 모리 리뉴얼(별 스티커, 부모 카드 카피)
9. idle 리셋 + 하드닝 + error boundary 자동복귀
10. Service Worker 프리캐시 + 이벤트 아웃박스

### Phase 2 — 파일럿 중 개선 (설치 후, 데이터 기반)
11. 완주율·퀴즈 완료율 보고 영상 V1 확장(+12편) 또는 길이 조정
12. 키오스크 선택값(mood/seed) → 가입 온보딩 프리필("이어짐" 체감)
13. 사서용 숨은 관리 제스처(구석 5탭 → 상태 화면: 버전·온라인·오늘 세션 수)
14. 퍼널 대시보드에 location 필터(운영 리포트용)

### 검증 (각 Phase 말)
- Phase 0: 파일럿 렌더 시사 — 5~7세 자녀 지인 1~2가정 반응 확인
- Phase 1: 실기기(구매한 탭) + Fully Kiosk에서 e2e — 오프라인 강제(비행기모드) 시나리오 포함, `lint`·`tsc`·`build` 그린
- 설치 전: 사내 리허설 + 아이 실사용 테스트 1회(하드웨어 계획 §6과 동기)

---

## 6. 일정 (하드웨어 계획 v2와 동기화, 8/31 바우처 데드라인 역산)

| 주차 | 앱/콘텐츠 (이 문서) | 하드웨어/바우처 (병행) |
|---|---|---|
| 7월 2주 | Phase 0: IP 에셋·영상 4편·퀴즈 저작 | 태블릿 구매·함체 발주 |
| 7월 3주 | Phase 1 개발 (4~10) | 함체 제작 중 |
| 7월 4주 | 실기기 e2e + 아이 테스트 → 수정 | 함체 수령·Fully Kiosk 세팅 |
| 8월 1주 | 설치 버전 태깅·배포 | 1호점 설치 |
| 8월 2주~ | Phase 2 (데이터 기반) | 파일럿 운영·주간 리포트 |

---

## 7. 리스크 & 열린 결정

- **세로(9:16) 영상 대응** — limited 파이프라인은 16:9 전제. 결정 필요: ⓐ 키프레임을 9:16으로 생성(프롬프트 수정, 권장) ⓑ 16:9 렌더 후 중앙 크롭(구도 손실) ⓒ 화면 상단 영상+하단 모리 프레임 UI(개발 추가). **ⓐ 권장** — 씬 구도 가이드만 세로용으로 보강.
- **모리 2D 셀 스타일 파생 미완** — R3는 아트토이 렌더 스타일. limited 키프레임과 톤이 붕 뜰 수 있음 → Phase 0 파일럿 렌더 1편으로 조기 판단, 어긋나면 바이블 다음단계 ②(2D 파생)를 먼저.
- **90초 회전율 vs 퀴즈 추가** — S4가 이탈을 만들면 역효과. 퀴즈 2문항 고정·스킵 허용·`quiz_skipped` 측정으로 파일럿에서 판단(제거가 아니라 데이터로 결정).
- **보급형 탭 성능** — limited 영상은 저비트레이트라 무리 없을 것이나, Phase 1 e2e를 반드시 실구매 기기에서.
- **오프라인 시 QR 어트리뷰션 저하** — §4 폴백 설계로 기능은 유지되나 세션 단위 연결 정밀도 하락. 도서관 Wi-Fi 품질을 섭외 단계 확인 항목으로(하드웨어 계획 §5 체크리스트에 이미 포함).
- **콘텐츠 안전 QA** — 5~7세 대상: 발행 전 휴먼 QA 게이트(방향전환 문서 원칙)를 키오스크 4편에도 동일 적용.
