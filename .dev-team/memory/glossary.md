# Glossary

Project-specific terms, module names, and conventions a worker needs to act
correctly without reading the whole repo. The planner inlines the relevant entries
into each package.

<!-- - <term>: <definition / where it lives>. Examples:
- "the ingest path": src/ingest/*, runs before normalization.
- "a tenant": a row in orgs; everything is scoped by tenant_id.
-->

-
