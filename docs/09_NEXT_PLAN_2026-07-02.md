# 다음 플랜 — 콘텐츠 생산 사이클과 런칭 활주로 (2026-07-02, Claude 검토 후 작성)

> 위치: docs/08 실행 완료(코덱스) + Claude 리뷰 커밋 `afaff38` 이후의 다음 단계.
> 이 시점 기준 **코드가 병목이 아니다.** P0 6건 중 코드 4건 해소, P1 18건 중 16건 해소,
> 인터랙티브 플레이어 최소 구현 완료. 남은 것은 (a) 콘텐츠 실물, (b) 운영 게이트 실행, (c) 실기기 QA다.
>
> Codex 실행노트(2026-07-02): 트랙 A-1 구현 완료. `LIBRARY_MATRIX_ANIMAL_VILLAGE` 6편을 추가하고
> `LIBRARY_MATRIX_90S` 기본값을 동물마을로 전환했다. 기존 미리 매트릭스는
> `LIBRARY_MATRIX_MIRI_DEPRECATED`로 보존. `public/ip/mori-reference-no-a.jpg`를 90초 파이프라인의
> 캐릭터 시트 생성 단계에 첨부하도록 `VideoBrief.referenceImagePaths`를 연결했고, 누락 시 유료 생성 전에 실패한다.
> `DRY_RUN=1 ANIMATION_MODE=limited LIMIT_COUNT=3 npx tsx --env-file=.env.local scripts/generate-library-episode-90s.ts`
> 확인 결과 첫 3편은 꾸미·방울·나옹 동물마을 영상, 예상 비용 총 `$3.31`.
> 트랙 D-1도 처리: 갱신 실패 메일은 첫 실패(active) 1통 + 만료 5일차 리마인더 1통으로 제한.
> 검증: `npx tsc --noEmit`, `npm run lint`, `npm run build`, `git diff --check` 통과.
>
> Claude 리뷰(2026-07-02): 매트릭스 캐스트·c6 분산·가드레일·메일 스로틀 로직 전부 정합 확인, DRY_RUN 재현($3.31).
> 보강 1건 — 레퍼런스 존재 검사가 director 호출($~0.8) 뒤에 있어 "유료 생성 전 실패" 주장과 어긋남 →
> 생성 스크립트 main()에 무비용 프리플라이트 추가. 잔여 메모: bespoke 경로(video-pipeline.ts 레퍼런스 단계)는
> 아직 모리 정본 미주입 — 트랙 D-2(bespoke 표면)와 함께 처리할 것.

## 현재 스코어보드

| 트랙 | 상태 |
|---|---|
| 코드 P0 | 4/4 해소 ✅ |
| 코드 밖 P0 | 콘텐츠 재고 3편(P0-4) · Inngest Cloud(P0-5) — **둘 다 대표 실행 대기** |
| P1 | 16/18 해소 (잔여: P1-8 실배치 완주, P1-9 음성 mp3 생성) · 실배치용 매트릭스 준비 완료 |
| 인터랙티브 플레이어 | 최소 구현 ✅ (placeholder 클립: 15초 영상 구간 분할) |
| 검증 | lint·tsc·build 그린 · `/`·`/demo/ai-diagnosis` 클릭 QA 완료 · **`/play` 로그인 QA 미완** |

## 트랙 A — 콘텐츠 생산 사이클 (최우선, 대표+다음 작업자 공동)

지금 유료 가치의 실체가 15초 클립 1편이다. 이 트랙이 곧 런칭이다.

1. ~~**동물마을 90초 매트릭스 작성** — `scripts/library-matrix-90s.ts`에 `LIBRARY_MATRIX_ANIMAL_VILLAGE` 추가(기존 미리 매트릭스는 deprecated 주석). 소재는 `docs/content/animal-village-season1.md` 시즌 계획에서, 캐스트는 꾸미·방울·나옹·도토·올빼미(승인 세계관), c6_focus 태깅 필수. **모리 정본 레퍼런스(public/ip/mori-reference-no-a.jpg) 주입**(docs/07 P2 — episode-pipeline.ts:437 빈 ref 문제) 코드 1줄 선행.~~ ✅ Codex 구현 완료. 생성 실배치 전 `DRY_RUN=1 ANIMATION_MODE=limited LIMIT_COUNT=3 ...` 통과.
2. **실배치 3편** (P1-8 완주): `ANIMATION_MODE=limited LIMIT_COUNT=3 npx tsx --env-file=.env.local scripts/generate-library-episode-90s.ts` — FAL/GOOGLE/ANTHROPIC 키 필요(대표). 예상 비용 편당 ~$1-2(limited).
3. **휴먼 QC** (RUNBOOK §콘텐츠 발행 체크): 캐릭터 일관성·한국어 음성 자연스러움·씬당 한 행동·용어 가드레일·모바일 재생.
4. **발행**: published=true SQL(RUNBOOK) → `/library`·`/play`에서 확인. 이때 '계속 늘어나는 모리 이야기' 카피를 '매주 새 이야기'로 되돌릴지 대표 결정.
5. **씬 그래프 실클립 연결**: 생성된 에피소드의 씬 타임스탬프(0013 scenes rows)로 `ANIMAL_VILLAGE_SCENE_GRAPH`의 placeholder 구간을 교체하거나, 후속으로 episode-pipeline의 씬별 클립(s0N.mp4) 출력(docs/07 C-3)을 붙여 진짜 가지 씬을 만든다. 가지/결말 씬 2-4개는 limited로 추가 생성(편당 추가 ~$0.5).
6. **음성 mp3** (P1-9 마무리): GOOGLE_API_KEY 채운 뒤 `npx tsx --env-file=.env.local scripts/gen-village-tts.ts` → `public/audio/village/*.mp3` 커밋(.gitignore는 이미 해제됨).

## 트랙 B — 실기기 QA (코드 밖 게이트 열기 전 필수)

1. **로그인 세션 `/play` 인터랙티브 플로우**: 선택 2회→결말→완료, `game_rounds` 저장 네트워크 확인(코덱스가 미완로 명시).
2. **iOS Safari 실기기**: ① 자동재생 — 거부 시 '톡 눌러서 이어 보기' 오버레이가 뜨는지(이번 리뷰에서 추가) ② 선택 프롬프트 음성 발화 ③ playsInline 인라인 재생 ④ 터치 반응.
3. **모바일 뷰포트**: 선택 오버레이 2열 그리드가 320-375px에서 잘리지 않는지, 진행/보상 표시.
4. 대시보드에서 인터랙티브 선택 기록이 '오늘 한 일'로 자연스럽게 보이는지(기록 계약은 동일하나 실표시 확인).

## 트랙 C — 코드 밖 게이트 실행 순서 (대표, RUNBOOK 절차)

`Inngest Cloud(P0-5) → Supabase prod 프로비저닝(db push, 0099는 manual로 격리됨) → BILLING_KEY_SECRET → Toss live 키+웹훅 등록·발화 테스트 → cloudbuild 실값 substitution 배포(^;^ 이스케이프) → RESEND_API_KEY(선택) → 20가구 소프트런칭`.
법무: privacy 국외이전·청약철회 문구 변호사 검토, 벤더 no-training 확인, CPO 자연인 지정.

## 트랙 D — 폴리시 (여유 시, 우선순위 순)

1. ~~갱신 실패 메일이 dunning 7일 동안 매일 발송됨 — 첫 실패 시 1통 + 5일차 리마인더 1통으로 완화(subscription-renewal의 notifyRenewalFailure에 발송 여부 조회 추가).~~ ✅ Codex 구현 완료. 별도 발송 로그 없이 subscription status/current_period_end 기준으로 active 첫 실패와 past_due 5일차만 발송.
2. bespoke 전달 표면: `/play`에 child 스코프 '맞춤 이야기' 섹션(docs/07 P2-③) — 20가구 콘시어지 시작 전 필요.
3. kiosk 익명 ingest 리미터, LibraryPostFlow 질문 사전 생성·캐시+음성, 레거시 /dashboard/study redirect (docs/07 4-B).
4. 프리미엄 프로바이더 A/B: KlingProvider 추가 → Seedance 동일 브리프 청취 비교(docs/07 C-2).
5. 성장 서사(주간 '처음 해낸 것') — 오퍼 루프의 마지막 ⬜ 조각(OFFER_MODEL_RECONCILE).

## 완료 정의 (소프트런칭 게이트)

발행 영상 ≥3편(QC 통과) · 인터랙티브 플로우 실기기 QA 통과 · Inngest cron 1회 실발화 확인 · Toss 웹훅 테스트 발화 반영 확인 · 프로드 배포 후 `/`→가입→온보딩→플레이→기록장→구독 E2E 1회 · 20가구 초대.
