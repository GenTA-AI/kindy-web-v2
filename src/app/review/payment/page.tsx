import PaymentReview from './PaymentReview';

export const metadata = {
  title: 'Kindy 월 구독 · 결제 경로 테스트',
  robots: { index: false, follow: false },
};

export default function PaymentReviewPage() {
  return <PaymentReview />;
}
