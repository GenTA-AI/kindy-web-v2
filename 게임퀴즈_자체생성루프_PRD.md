# 게임·퀴즈 자체생성 루프 — PRD

> 작성일 2026-06-04 · 목적: 1회 교육 세션 **20분**을 "20분 영상"이 아니라 **꿈의정원(Gardenscapes)式 자체생성 게임 + 퀴즈 루프**로 채운다. 비용·교육 문제를 동시에 푼다.
> 연계: `런칭_꿈샘_데이터측정_스펙.md`(신호 수집), `IR_DECK.md`(GACS-3·flywheel), `AI책놀이_데모_UIUX기획.md`(키오스크 S4)

---

## 1. 문제 — 왜 "20분 영상"이 답이 아닌가

목표는 한 번에 약 20분, 아이를 학습에 붙잡아 두는 것. 그런데 이를 **영상으로** 채우면 두 가지가 동시에 깨진다.

**(1) 비용** — 영상 1편(15초) 실측 $4.94(STATUS.md). 20분이면 단순 환산 약 **80배 = 세션당 수십 달러**. ARPU 월 25,000원(≈$18) 구독에서 세션당 수십 달러 COGS는 즉시 적자. 영상은 "짧고 강한 후크"여야지 시간을 때우는 매체가 될 수 없다.

**(2) 교육** — 20분 수동 시청은 미취학 아동에게 ① 주의력 분산·수동성, ② screen-time 거부감(부모), ③ 학습 효과 측정 불가(보기만 함)를 초래한다. IR의 PROBLEM은 정확히 "screen time 슬롯이 비어있다"였다 — 우리가 채울 슬롯은 *수동 영상*이 아니라 **능동 학습**이다.

> 결론: 영상은 **후크(30~90초)**, 시간의 대부분은 **자체생성 게임·퀴즈**가 채운다.

---

## 2. 컨셉 — "끝나지 않는 작은 학습 놀이터"

꿈의정원이 끝없이 새 레벨을 만들어 내듯, 우리는 **아이의 취향 프로필(GACS-3)을 시드로 끝없이 새 게임·퀴즈를 만든다.** 영상 1편이 신호 수십 개라면, 게임 세션 1회는 **신호 수백 개**다 — flywheel(데이터→콘텐츠 fit→몰입)이 한 단계 빨라진다.

```
세션(≈20분) 구조
[0–1분]  후크 영상(사전 제작/캐시) — 오늘의 주제·캐릭터 등장
[1–18분] 자체생성 루프  ← 핵심
          라운드 = 미니게임 1 + 퀴즈 1~2  (45~90초)
          정답률·속도 → 다음 라운드 난이도·주제 적응
          별/스티커 보상, 컬렉션 채우기(꿈의정원式 메타 진행)
[18–20분] 마무리 — 획득물 요약 + (가정)부모 리포트 / (키오스크)QR
```

핵심 원칙:
- **자체생성 = 저비용**: 게임·퀴즈는 템플릿×파라미터로 클라이언트에서 즉시 생성(LLM 호출은 라운드 단위 소량·캐시). 영상 생성비와 자릿수가 다르다.
- **적응형**: 정답률 높으면 난이도↑·새 주제, 낮으면 복습·힌트. = 학습 + GACS-3 신호.
- **메타 진행(retention loop)**: 별→스티커→컬렉션→캐릭터 성장. 꿈의정원의 "한 판 더" 동력을 학습에 이식.

---

## 3. 게임 유형 (게임 개발자 합류 전제 — 최소 세트)

미취학 대응: 글 최소·그림 위주, 큰 터치 타겟, 정답에 축하 마이크로 애니메이션, 오답도 부드럽게("거의 다 왔어!"). 모두 **주제(과학/영어/한글/노래)·캐릭터로 스킨**된다.

| # | 유형 | 학습 | 자체생성 파라미터 |
|---|---|---|---|
| G1 | 짝맞추기/기억 | 어휘·개념 | 카드 세트(주제 자산), 장수, 제한시간 |
| G2 | 분류·드래그 | 범주화(과학) | 보기 풀, 정답 분류 수 |
| G3 | 순서맞추기 | 시퀀스·한글 자모 | 시퀀스 길이, 셔플도 |
| G4 | 듣고 고르기 | 영어·노래(TTS 재사용) | 오디오-보기 매핑 |
| G5 | 찾기(숨은그림) | 관찰·집중 | 오브젝트 수, 밀집도 |
| Q  | 퀴즈(그림/선택) | 후크 영상 연계 | 문항 수, 난이도, 오답 보기 생성 |

> 게임 개발자 역할: ① 템플릿 엔진(파라미터→플레이 가능한 라운드) ② 난이도 적응 로직 ③ 메타 진행/보상 시스템 ④ 9:16 키오스크 + 모바일 공통 렌더. **콘텐츠가 아니라 "생성기"를 만든다**가 핵심.

---

## 4. 자체생성 아키텍처

```
취향 프로필(GACS-3: mood·style·tone·context) ─┐
주제·캐릭터·난이도 상태 ───────────────────────┼─▶ 라운드 플래너
                                              │     ├ 게임 유형 선택(가중치)
콘텐츠 자산 풀(이미지·TTS·어휘 뱅크) ──────────┘     ├ 파라미터 샘플링
                                                    └ (필요시)LLM 문항 생성·캐시
                                                          │
                                                          ▼
                                        플레이 → 정답률·속도·재도전 신호
                                                          │
                                                  난이도/주제 적응(bandit)
                                                          │
                                                  kiosk_events / view·quiz_results 적재
```

- **비용 통제**: 라운드의 95%+는 템플릿×자산 풀로 LLM 없이 생성. LLM은 새 문항 시드만, 결과는 캐시·재사용(북메이트 "READY 재사용=비용 0" 원칙 차용).
- **GACS-3 연계**: 게임 난이도 적응 궤적·정답률이 곧 감성/학습 좌표 갱신 입력 → 다음 후크 영상·게임 fit 향상.
- **안전(아동)**: 북메이트 QC 게이트(safetyGate·typography) 자산을 게임 텍스트/이미지에도 적용. 보안문서 정책(요약·결말 금지) 준수.

---

## 5. 측정 (데이터 스펙과 연결)

새 이벤트(설계 문서 `kiosk_events.event_type` 확장): `game_started` · `game_round_completed`(round·score·difficulty·latency) · `game_completed` · `collection_progress`.

| 신호 | 지표 | 사업 의미 |
|---|---|---|
| 라운드 정답률·속도 | 학습 효과 곡선 | "교육이 된다"의 증거 |
| 자발적 재도전 | 흥미 지속 | 20분 lock-in 강도 |
| 난이도 적응 궤적 | 개인화 fit | flywheel 작동 증명 |
| 세션 체류(20분 대비) | 몰입도 | 경쟁 키즈앱 대비 해자 |

---

## 6. 단계별 범위

- **MVP(꿈샘 런칭)**: 후크 영상 + 퀴즈(Q) + 게임 2종(G1·G2) + 별/스티커 보상 + 이벤트 적재. 키오스크 S4를 이걸로 교체.
- **V1(가정)**: 컬렉션·캐릭터 성장(retention loop), G3~G5 추가, 난이도 bandit 본격화, 부모 리포트에 "학습 곡선" 노출.
- **V2(2단계 포지셔닝)**: 음악·예술 모드(리듬 탭·색칠·작곡 미니게임) — 강남 타깃 예술교육 플랫폼의 코어 인터랙션으로 확장(마스터플랜 §2단계 참조).

---

## 7. 리스크
- 자체생성 품질 편차 → 템플릿·QC 게이트로 하한 보장, 라운드 사용자 평가 신호로 가중치 학습.
- 게임이 "학습 없는 오락"으로 흐름 → 모든 라운드에 학습 목표 태그 필수, 부모 리포트에 학습 항목 명시.
- 개발 리소스 → 생성기/엔진 우선(콘텐츠 양산 대신), 시드 use-of-funds "1–2 개발자" 안에서 게임 개발자 1명 충원.

---

## 부록: 비용 비교(실측 기반)
- 20분 영상(가정): 15초 $4.94 × 80 ≈ **세션당 ~$395** — 비현실.
- 후크 영상(캐시·재사용) + 자체생성 게임 루프: 영상은 세션당 거의 0(사전 제작 재생), 게임은 템플릿 생성으로 LLM 호출 최소 → **세션 COGS를 ARPU 안으로** 들여놓는다.
