# 정서·창의성 주간 자동 생성 커리큘럼 — 기획

> 작성 2026-06-07 · 목적: 과목(과학·영어·한글) 대신 **정서(SEL)** 와 **창의성** 두 트랙을 코어로, "닭 마을 세계관"의 **연속 서사가 매주 자동으로 다음 이야기로 이어지며 생성·교육**되는 시스템을 설계한다.
> 전제 포맷: 주 2~3회, 회당 ~20분, **약 3분 영상 2개 + 사이사이 퀴즈·게임**(기존 `영상후_게임퀴즈루프_구현계획.md`의 20분 세션 청사진 그대로).
> 연계 기존 문서: `게임퀴즈_자체생성루프_PRD.md` · `영상후_게임퀴즈루프_구현계획.md` · `런칭_마스터플랜.md` · `docs/research/SYNTHESIS-design-principles-2026-06-04.md`

---

## 0. 한 장 요약

핵심 통찰: **이미 만들어 둔 두 엔진을 "주간 연속 서사"로 묶기만 하면 된다.**

1. **커리큘럼 척추**(`0014_syllabus.sql`: syllabuses→units→lessons + enrollment.cadence + lesson_progress) — *과목 무관(subject-agnostic)* 으로 이미 설계됨. 그대로 `subject='sel_emotion'`, `subject='creativity'` 두 벌을 얹으면 끝.
2. **자체생성 게임·퀴즈 루프**(`engine.planSession`, `emotion_expression` 게임 포함) — 영상 사이 20분을 LLM 없이 채우는 부분은 이미 구현·리뷰 완료.

빠져 있는 단 하나의 조각 = **"매주 다음 화를 스스로 만들어 내는" 생성 오케스트레이터**다. 지금 영상 생성은 운영자가 손으로 돌리는 batch 스크립트(`generate-library-episode-90s.ts`)뿐이고, **연속성(이번 주가 지난 주를 이어받는다)** 과 **자동 스케줄**이 없다.

이 문서가 더하는 것:
- **2트랙 시즌제 커리큘럼**: 정서=CASEL 5역량→누리 사회관계, 창의성=Torrance 4요소→누리 예술경험.
- **스토리 캐논(Story Bible) 상태**: 세계관·캐릭터·플롯·학습 진도를 한 곳에 적재해, LLM이 **읽고→이어쓰고→되쓰는** 연속성 장치.
- **주간 생성 cron**(Inngest): 토픽 플래너 → 대본 → 영상(기존 파이프라인) → 게임/퀴즈 파라미터 → **휴먼 QA 게이트** → 발행 → 자동 차시 편성.

> 결론: "무한히 다음 이야기"는 **(a) 커리큘럼 scope-and-sequence가 *무엇을 배울지*를, (b) 스토리 캐논이 *서사가 어디까지 왔는지*를 고정**하고, LLM은 그 둘 사이의 창작 빈칸만 채우게 하면 드리프트 없이 자동화된다.

---

## 1. 현재 자산 ↔ 갭

| 필요한 것 | 이미 있는 것 | 갭 |
|---|---|---|
| 커리큘럼 구조(단원/차시/진도/주기) | `0014_syllabus.sql` 5테이블 + `src/types/syllabus.ts` + `enrollments.cadence.lessons_per_week` | 정서·창의성 시드 콘텐츠 없음 (현재 시드는 `korean_reading` 한 벌) |
| 20분 능동 세션(영상→게임→퀴즈) | `engine.ts`·`SessionShell.tsx`·`PuzzleGame`·`EmotionExpressionGame`·`RewardMeta` (11패키지 완료) | 정서·창의성 학습목표 태그·자산 풀 보강 필요 |
| 영상 생성 | `episode-pipeline.ts`(Claude director→nano-banana→Seedance→Gemini TTS→lipsync), `generate-library-episode-90s.ts` batch | **연속 서사 입력 없음**(매 brief가 독립) · **자동 스케줄 없음**(수동 실행) |
| 학습목표↔교육과정 매핑 | `curriculum-tags.ts`(`sel_emotion_awareness` 존재) | CASEL/Torrance 세부 코드 미정의 |
| 부모 가치 리포트 | `sel-report.ts` + `/dashboard/report` | 창의성 트랙 지표 미포함 |
| 비동기 잡 인프라 | `src/inngest/functions/video-generation.ts` + `/api/inngest` | **`INNGEST_DEV=1`**(프로덕션 미연결) · 주간 cron 함수 없음 |

→ **신규 작업은 "생성 오케스트레이터 + 2트랙 시드 + 연속성 상태"에 집중**. 렌더·게임·영상 기술 스택은 재사용.

---

## 2. 커리큘럼 재설계 — 2트랙 시즌제

### 2-1. 왜 정서·창의성인가 (포지셔닝 정합)
`런칭_마스터플랜.md` 2단계 전략이 이미 **"기질 맞춤 정서 엔진 → 음악·예술 표현 교육"** 으로 가도록 설계돼 있다. 정서+창의성은 그 2단계 wedge를 **1단계 코어로 끌어올린 것** — 학부모 결제 동기(정서·표현)와도, GACS-3 좌표(mood·style·tone)와도 가장 잘 맞는다. SYNTHESIS 근거상으로도 음악/정서 트랙이 단독 근거가 가장 강하다(공감·친사회성↑).

> 메시징 가드레일(근거 D6, 변경 없음): **"사회정서 *기술*을 연습"** O / "기분이 좋아져요·정서가 안정돼요" ❌ / 의학·DTx·ADHD 클레임 ❌. 창의성도 "확산적 사고를 *연습*" 으로 표현, "영재성" 단정 금지.

### 2-2. 정서 트랙 — CASEL 5 → 누리 '사회관계'
CASEL 5역량을 시즌(=단원)으로, 주차 테마(=차시 묶음)로 분해. 각 주차는 *관찰 가능한 한 가지 기술*에 집중(누리과정 '나를 알고 존중하기 / 더불어 생활하기'에 매핑, `curriculum-tags`의 `standard_source:'nuri'` 유지).

| 시즌(단원) | CASEL 역량 | 주차 테마 예시 | 닭 마을 서사 훅 |
|---|---|---|---|
| S1 내 마음 알기 | 자기인식 | 기쁨·슬픔·화남·무서움 알아채기, "두 마음이 동시에" | 꼬꼬가 처음 마을에 와서 낯선 감정을 만남 |
| S2 마음 다루기 | 자기관리 | 화날 때 멈추기('멈추고-생각하고-고르기'), 기다리기 | 알 깨기 대회에서 조급함을 다스리는 꼬꼬 |
| S3 친구 마음 알기 | 사회적 인식 | 표정 읽기, 공감, "친구가 울면?" | 새 친구 삐약이의 기분 알아채기 |
| S4 같이 잘 지내기 | 관계 기술 | 나눔, 차례 지키기, 갈등 풀기 | 마을 잔치 준비하며 다투고 화해 |
| S5 좋은 선택 | 책임 있는 결정 | 안전·배려 선택, 결과 생각하기 | 위험한 숲 길 앞에서 선택 |

> Daniel Tiger 모델 차용: **에피소드 = 같은 테마의 두 세그먼트 + 반복되는 "전략"**(예: "멈추고, 생각하고, 골라요" 같은 짧은 전략 송/주문). 우리 세션의 영상 2개가 정확히 이 두 세그먼트, 게임 사이에서 그 전략을 *연습*시킨다.

### 2-3. 창의성 트랙 — Torrance 4요소 → 누리 '예술경험'
정답 없는 **과정 중심(process>product)**. 확산적 사고 4요소를 주차 미션으로.

| 시즌(단원) | Torrance 요소 | 주차 미션 예시 | 게임 형식 |
|---|---|---|---|
| C1 아이디어 많이 | 유창성(fluency) | "이걸로 뭘 할 수 있을까?" 많이 떠올리기 | 브레인스토밍형(정답 없음, 참여도 점수) |
| C2 다르게 보기 | 융통성(flexibility) | 한 사물을 여러 용도로, 관점 바꾸기 | 분류·재배치 변형 |
| C3 나만의 것 | 독창성(originality) | 미완성 그림 완성, 새 캐릭터 만들기 | 드로잉/조합 창작 |
| C4 더 풍부하게 | 정교성(elaboration) | 이야기에 살 붙이기, 꾸미기 | 시퀀스+꾸미기 |

> 창의성 게임은 옳고/그름이 없으므로 `emotion_expression` 형식(참여도 기반 점수)을 **창작형(creative_expression)으로 일반화**해 재사용. "What else / What if" 오픈 질문을 영상·게임 프롬프트 표준으로.

### 2-4. 시즌·주차 운영 모델 (주 2~3회와 맞물림)
- **한 주 = 정서 1편 + 창의성 1편**(주 2회) 또는 +복습/심화 1회(주 3회).
- **시즌 = 8~12주차**(syllabus_unit), 시즌 종료 시 다음 시즌 자동 진입.
- `enrollments.cadence.lessons_per_week`로 아이마다 2/3회 선택 → `lesson_progress`가 잠금/해제 제어(기존 스키마 그대로).

---

## 3. 콘텐츠 단위 모델 — 기존 syllabus에 그대로 매핑

```
시즌 (syllabus_unit)
  └─ 주차 에피소드 (syllabus_lesson 1개 = 아이의 1회 ~20분 세션)
        ├─ 영상 A (~3분, 후크+테마 도입)         ← library_videos (episode pipeline 산출)
        ├─ 게임/퀴즈 라운드 (engine.planSession) ← LLM 0, 템플릿×파라미터
        ├─ 영상 B (~3분, 전략/미션 적용)          ← library_videos
        └─ 클로저(정서 표현 / 창작 산출 + 보상)   ← EmotionExpression/Creative + RewardMeta
```

- **정서/창의성 = 두 개의 `syllabuses` 행**(`subject='sel_emotion'` / `'creativity'`, `age_band` 5·6·7로 시즌 묶음).
- 주차 에피소드 = `syllabus_lesson`. 영상 2개를 한 차시에 묶기 위해 **차시당 영상 2개 매핑이 필요** → 아래 §9의 스키마 보강(차시-영상 다대일) 또는 한 차시를 "세그먼트 2"로 보는 `lesson.meta` jsonb 활용.
- 진도·주기·잠금은 **추가 개발 없이** 기존 `lesson_progress` + `cadence`로 동작.

---

## 4. 연속 서사 엔진 — "다음 이야기"가 끊기지 않게 (핵심)

자동 생성에서 가장 어려운 건 영상 품질이 아니라 **연속성**: 12주차에도 캐릭터·설정·이미 가르친 것과 모순되지 않아야 한다. LLM 단독 생성은 반드시 표류한다. 해법은 **세 개의 진실 원천을 분리**하는 것:

1. **무엇을 배울지** = 커리큘럼 scope-and-sequence(§2, 고정). → "이번 주는 C2 융통성."
2. **서사가 어디까지 왔는지** = **스토리 캐논(Story Bible 상태)** (아래, 가변·append-only).
3. **창작 빈칸** = LLM이 1·2 사이를 채움(매주 새로움은 여기서만 발생).

### 4-1. 스토리 캐논(Story Bible) 상태
세계관의 "정본"을 구조화 데이터로 보관. 생성기가 **매주 읽고 → 이번 화 산출 → 새 사실을 되쓴다(append).**

```jsonc
// story_canon (신규 테이블 또는 syllabus.meta jsonb)
{
  "world": { "name": "꼬꼬 마을", "rules": ["마을엔 알 깨기 대회가 있다", "..."] },
  "characters": [
    { "id": "kkokko", "name": "꼬꼬", "traits": ["호기심", "겁많음→성장중"],
      "ref_image_ids": ["nano:kkokko_v1"], "arc_state": "S1 감정 어휘 배우는 중" }
  ],
  "plot_threads": [ { "id": "festival", "status": "준비중", "opened_week": 4 } ],
  "taught_ledger": [ { "objective": "sel_self_awareness_joy", "week": 1 } ],  // 중복 방지
  "recap_last": "지난 주 꼬꼬는 삐약이의 슬픔을 알아챘다.",
  "season": { "track": "sel_emotion", "code": "S3", "week_in_season": 2 }
}
```

핵심 규칙(생성 프롬프트에 강제):
- 캐릭터 외형은 **캐논의 `ref_image_ids`만 사용**(매주 재생성 금지) → 일관성 + 비용 절감.
- `taught_ledger`로 **이미 가르친 목표 재출제 방지**(복습은 명시적 플래그일 때만).
- 새 캐릭터·설정은 캐논에 *추가*만 가능, 기존 사실 *수정 금지*(append-only) → 모순 차단.

### 4-2. 주간 토픽 플래너 (LLM, 주 1회)
입력: 캐논 상태 + scope-and-sequence의 다음 목표 + 직전 주 engagement 집계(정답률·재시도·정서표현 참여도) →
출력: **에피소드 브리프**(테마, 학습목표+`curriculum_tag`, 서사 비트 4~7개, 등장/신규 캐릭터, "전략 송/상상 퀘스트" 훅, 게임에 쓸 어휘·감정 아이템 풀). 이 브리프가 기존 `VideoBrief`/`EpisodeScript`로 들어간다.

> engagement 반영 = `런칭_마스터플랜` flywheel과 동일 철학: 정답률 낮은 목표는 난이도↓·복습 주차 삽입, 특정 캐릭터 반응이 좋으면 등장 가중. (V1; MVP는 순차 진행만.)

---

## 5. 주간 자동 생성 파이프라인 (Inngest cron)

신규 함수 `src/inngest/functions/weekly-episode-generator.ts`. **주 1회 cron**(예: 콘텐츠 버퍼 4~6주 유지하도록 미리 생성). 단계는 기존 `episode-pipeline.ts`를 그대로 호출.

```
[cron weekly]
  step.1 캐논 로드 + 다음 목표 결정 (scope-and-sequence 커서)
  step.2 토픽 플래너(LLM) → 에피소드 브리프 (정서 1 + 창의성 1)
  step.3 연속성 검증: taught_ledger 중복·캐릭터 모순 자동 체크 (실패 시 재프롬프트 1회)
  step.4 대본: ClaudeDirector → EpisodeScript ×2 (영상 A/B, 각 ~3분)
  step.5 영상: 캐릭터 ref 캐시 재사용 → Seedance + Gemini TTS + lipsync (기존 파이프라인)
  step.6 게임/퀴즈 파라미터: engine이 브리프의 어휘·감정 풀에서 라운드 구성 (LLM 0)
  step.7 published=false 로 library_videos + syllabus_lesson(준비중) 적재
  step.8 ── HUMAN QA 게이트 ── (안전·풀이가능성·연령 적합성 승인)
  step.9 승인 시 published=true 토글 + 캐논 append(recap/ledger/신규캐릭터) + 차시 해제
```

- **결정적 부분(게임·진도)은 LLM 0**, **창작 부분(대본·플롯)만 LLM** — 기존 PRD의 "핫패스 LLM 0" 원칙 유지.
- step.5 캐릭터 ref 재사용이 비용·일관성의 핵심(§6).
- step.8을 처음엔 사람이, 이후 **AI 사전 스크리닝(안전 분류기) + 사람 예외 확인**으로 완화(근거 C3: 자동생성 품질은 휴먼 QA 루프 필수).

> 인프라 선행: `런칭_마스터플랜` 체크리스트 C의 **Inngest Cloud 연결**(`INNGEST_DEV=1` 해제)이 자동 스케줄의 전제. 그 전까지는 같은 파이프라인을 운영자가 주간 수동 실행(batch)으로 돌려 동일 산출을 낼 수 있다.

---

## 6. 비용·캐싱 전략 (현실성)

비용 드라이버는 영상이다(실측 15s $4.94 중 Seedance $4.54 — `STATUS.md`). "매주 완전 신규 영상"을 순진하게 하면 적자. 통제책:

1. **캐릭터 ref 1회 생성·영구 재사용** — nano-banana 캐릭터 키프레임은 캐논에 박아 두고 매주 *배경·동작 씬만* 신규. (일관성도 같이 해결)
2. **버퍼 생성(실시간 아님)** — cron은 "다음 주"가 아니라 "4~6주 뒤"를 미리 만들어 QA 시간 확보 + 실패 재시도 여유.
3. **게임 루프가 시간의 대부분** — 20분 중 영상은 ~6분(후크), 나머지는 LLM 0 게임(기존 PRD 그대로) → 세션 COGS를 ARPU(월 25,000원) 안으로.
4. **배경/소품 라이브러리 리믹스** — 자주 쓰는 마을 배경은 재사용 풀로.
5. **트랙당 주 1편씩**(주 2편/주)만 신규 → 라이브러리는 시간이 갈수록 *복습·재시청* 자산으로 누적(근거 B3: 같은 편 반복 시청이 이해도↑ → 신규 압박 완화).

---

## 7. 안전·QA·법무

- **휴먼 QA 게이트 필수**(근거 C3, COPPA). 5~10세 대상이므로 자동 발행 금지. 북메이트 `safetyGate`·typography QC를 영상·게임 텍스트/이미지에 적용(기존 PRD §4).
- **연속성 검증**(step.3): 캐논 모순·중복 출제 자동 체크.
- **메시징 가드레일**(§2-1): SEL "기술 연습"·창의성 "과정", 효능 과장·의학 클레임 금지.
- 법무 선행은 `런칭_마스터플랜` 체크리스트 C(사업자·약관·개인정보)와 동일 — 유료 전환 측정 전 마감.

---

## 8. 측정 (트랙별 신호 → 부모 리포트)

기존 `game_*` 이벤트 + `lesson_progress`를 트랙별로 집계. `sel-report.ts`를 확장:
- 정서: "이번 주 공감/감정 알아채기 활동 N회"(관찰 가능한 연습량, 내면 상태 단정 X — 근거 D7).
- 창의성: "아이디어 N개 냄 / 나만의 작품 N개"(유창성·독창성 *연습량*).
- flywheel: 트랙별 정답률·참여도 → 토픽 플래너(§4-2) 입력으로 되먹임.

---

## 9. 구현 로드맵 (기존 `.ai/packages` 컨벤션)

다음 마이그레이션 시퀀스 = **`0017`**(현재 최신 `0016_game_events.sql`).

### Phase 0 — 2트랙 커리큘럼 시드 (영상·생성 없이 즉시)
- `db-story-canon` → `supabase/migrations/0017_story_canon.sql`: `story_canon`(jsonb 상태, 카탈로그성 RLS) + `syllabus_lessons`에 영상 2개 매핑용 `meta jsonb` 또는 `lesson_videos` 조인.
- `seed-syllabus-sel-emotion` / `seed-syllabus-creativity` → `src/data/syllabus-sel-emotion.ts`·`syllabus-creativity.ts` + `scripts/seed-syllabus.ts` 확장 (기존 `seed-syllabus-korean-preschool` 패턴 그대로, delete-then-insert idempotent).
- `lib-curriculum-tagging` 확장: `curriculum-tags.ts`에 CASEL 5 + Torrance 4 목표 코드 추가(`standard_source:'nuri'`, `interpretive:true`).
- `data/games/asset-pools`: `emotion` 풀 보강 + `creativity` 풀 신규.

### Phase 1 — 생성 오케스트레이터 (MVP: 수동 트리거)
- `lib-topic-planner` → `src/lib/content/topic-planner.ts`: 캐논+커서+engagement → 에피소드 브리프(LLM).
- `lib-continuity-guard` → `src/lib/content/canon.ts`: 캐논 read/append + 모순·중복 검증.
- `script-episode-3min`: `episode-pipeline.ts`를 ~3분×2 세그먼트로 호출하는 래퍼 + 캐릭터 ref 캐시 재사용.
- `ops-weekly-generate` → `scripts/generate-weekly-episode.ts`(batch, 운영자 수동 — Inngest 전 단계 동일 산출). published=false.

### Phase 2 — 자동 스케줄 + 되먹임 (Inngest Cloud 후)
- `infra-inngest-cloud`(체크리스트 C) 완료 → `inngest/functions/weekly-episode-generator.ts` cron.
- `feat-qa-gate`: 어드민에서 생성물 검토·발행 토글 + AI 사전 스크리닝.
- `parent-report-creativity`: `sel-report.ts`에 창의성 지표 + 토픽 플래너 engagement 되먹임.

### Phase 3 — V1 고도화
- 난이도 bandit/DDA(근거 C4), 캐릭터 등장 가중 학습, 시즌 자동 전환, 배경 리믹스 라이브러리.

---

## 10. 리스크 & 대응

| 리스크 | 대응 |
|---|---|
| LLM 서사 표류(캐릭터·설정 모순) | 스토리 캐논 append-only + step.3 연속성 검증 + 캐릭터 ref 고정 |
| 영상 비용 폭발 | ref 재사용·버퍼 생성·게임이 시간 대부분·트랙당 주 1편 (§6) |
| 자동 발행 안전 사고 | 휴먼 QA 게이트 필수, AI는 사전 스크리닝까지만 |
| "교육 효과" 근거 빈약(세션·길이 수치) | 20분·3분은 가설로 명시, A/B 검증(근거 E1). 부모 메시징은 '기술 연습'으로 한정 |
| Inngest 프로덕션 미연결 | MVP는 수동 batch로 동일 산출 → 인프라는 병렬로 |
| 정서·창의성 한국 표본 근거 한계 | 누리/2022 매핑은 해석적 bridge로 표기(근거 F) |

---

## 부록 A. 근거 출처
- 내부: `docs/research/SYNTHESIS-design-principles-2026-06-04.md`(A~F), `game-formats-20min-session-2026-06-05.md`, `korea-curriculum-alignment-2026-06-05.md`.
- 외부: CASEL 5역량(self-awareness/self-management/social awareness/relationship skills/responsible decision-making), Second Step Early Learning(주 28테마·일 5~7분), Daniel Tiger(에피소드=공통 SEL 테마 2세그먼트+전략 송), Torrance 창의성 4요소(유창성·융통성·독창성·정교성, 과정>산물).

## 부록 B. 핵심 파일 맵 (재사용/신규)
| 역할 | 기존 파일(재사용) | 신규 |
|---|---|---|
| 커리큘럼 DB | `supabase/migrations/0014_syllabus.sql`, `src/types/syllabus.ts` | `0017_story_canon.sql` |
| 시드 | `scripts/seed-syllabus.ts`, `src/data/syllabus-korean-preschool.ts` | `syllabus-sel-emotion.ts`, `syllabus-creativity.ts` |
| 영상 생성 | `src/lib/episode-pipeline.ts`, `scripts/generate-library-episode-90s.ts` | `scripts/generate-weekly-episode.ts`, `src/lib/content/topic-planner.ts`, `canon.ts` |
| 게임/퀴즈 | `src/lib/game/engine.ts`, `src/data/games/asset-pools.ts`, `SessionShell.tsx` | `creativity` asset pool, `creative_expression` 게임 |
| 교육과정 태그 | `src/lib/game/curriculum-tags.ts` | CASEL/Torrance 코드 추가 |
| 스케줄 | `src/inngest/functions/video-generation.ts`, `/api/inngest` | `weekly-episode-generator.ts`(cron) |
| 부모 리포트 | `src/lib/game/sel-report.ts`, `/dashboard/report` | 창의성 지표 |
