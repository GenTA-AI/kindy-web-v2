# 방향 전환 핸드오프 — 과목형 → 정서·창의성 연속 서사

> 작성 2026-06-07 · **대상 독자: 이 레포를 개발 중인 코딩 에이전트(claude-planner / claude-worker)와 사람 리드.**
> 목적: 제품의 *콘텐츠 방향*을 "과목별 영상(과학·영어·한글)"에서 **"정서(SEL) + 창의성 2트랙의, 매주 자동으로 이어지는 닭 마을 연속 서사"** 로 전환한다. 이 문서는 변경의 **FROM→TO·재사용·신규·작업 백로그**를 한 곳에 정리한 단일 진입점이다.
> 상세 설계: `정서창의성_주간자동생성_기획.md`(아키텍처) · `게임퀴즈_자체생성루프_PRD.md` · `영상후_게임퀴즈루프_구현계획.md` · `docs/research/SYNTHESIS-design-principles-2026-06-04.md`(근거).

---

## 0. TL;DR (에이전트용 30초 브리핑)

- **무엇이 바뀌나**: 콘텐츠 축이 *과목*(`science`/`english`/`hangul`)에서 *역량 2트랙*(`sel_emotion` / `creativity`)으로 바뀐다. 영상은 **독립 에피소드 → 연속 서사**(꼬꼬 마을, 매주 다음 화)로 바뀐다. 생성은 **운영자 수동 batch → 주간 자동(스토리 캐논 기반)** 으로 진화한다.
- **무엇이 안 바뀌나(중요)**: `0014_syllabus.sql`(subject-agnostic 척추), 20분 게임·퀴즈 루프 11패키지(`engine.ts`/`SessionShell`/`PuzzleGame`/`EmotionExpressionGame`/`RewardMeta`), 영상 파이프라인(`episode-pipeline.ts`), RLS·인증 패턴, "핫패스 LLM 0" 원칙. **삭제·리팩터 대상 아님 — 위에 얹는다.**
- **지금 진행 중인 작업과의 관계**: `korean_reading` 시드와 `library-matrix(-90s).ts`의 과목 매트릭스는 **legacy로 유지(삭제 금지)**. 신규 2트랙은 *추가*로 시드한다. 매트릭스(독립 brief 나열) 개념은 새로 **캐논+토픽 플래너(연속 brief 생성)** 로 대체된다.
- **다음 한 수**: Phase 0 — `0017_story_canon.sql` + 정서·창의성 시드 + 교육과정 태그 확장. 영상·LLM 없이 가능, 나머지의 토대.

---

## 1. FROM → TO 비교표

| 축 | FROM (현재 구현) | TO (전환 후) |
|---|---|---|
| 콘텐츠 분류 | 과목: science / english / hangul / music | **2트랙: 정서(sel_emotion) · 창의성(creativity)** |
| `syllabuses.subject` | `'korean_reading'` 1벌 (시드) | `'sel_emotion'`, `'creativity'` 2벌 추가 (과목 시드는 legacy 유지) |
| 영상 단위 | 독립 에피소드 (matrix의 brief 각각 무관) | **연속 서사** — 매주 지난 화를 이어받음 (꼬꼬 마을 세계관) |
| 생성 트리거 | 운영자 수동 `npx tsx scripts/generate-library-episode-90s.ts` | (MVP) 수동 batch 유지 → (V1) **주간 Inngest cron** |
| 서사 일관성 | 없음 (brief마다 독립) | **스토리 캐논(story_canon)**: 캐릭터·플롯·학습 진도 append-only 상태 |
| 다음 화 결정 | `library-matrix(-90s).ts` 정적 배열 | **토픽 플래너(LLM)**: 캐논 + scope-and-sequence 커서 + engagement → 브리프 |
| 학습목표 태그 | `sel_emotion_awareness` 등 일부 | CASEL 5 + Torrance 4 목표 코드로 확장 |
| 발행 | `published=false` insert → 사람이 토글 | 동일 + **휴먼 QA 게이트 명문화**(5~10세 안전 필수) |

---

## 2. 도메인 모델 (전환 후)

### 2-1. 2트랙 = 2개의 `syllabuses`
- 정서: `subject='sel_emotion'` — CASEL 5역량을 시즌(`syllabus_units`)으로. 누리 '사회관계'에 매핑(`standard_source:'nuri'`).
- 창의성: `subject='creativity'` — Torrance 4요소(유창성·융통성·독창성·정교성)를 시즌으로. 누리 '예술경험'에 매핑. **정답 없음 = 참여도 점수.**
- 진도·주기·잠금은 기존 `lesson_progress` + `enrollments.cadence.lessons_per_week`로 **추가 개발 없이** 동작.

### 2-2. 주차 에피소드 = `syllabus_lesson` (아이의 1회 ~20분 세션)
한 차시 = **영상 2편(~3분) + 게임/퀴즈 + 클로저**. 차시당 영상 2개 매핑이 필요하므로 §4 스키마 보강 참조.

### 2-3. 연속성의 3-분리 (드리프트 방지 핵심)
LLM 단독 생성은 12주차쯤 캐릭터·설정이 모순된다. 진실 원천을 셋으로 나눈다:
1. **무엇을 배울지** = scope-and-sequence(고정, §3) — 토픽 플래너의 커서.
2. **서사가 어디까지 왔는지** = `story_canon`(가변, append-only) — 캐릭터/플롯/`taught_ledger`/recap.
3. **창작 빈칸** = LLM이 1·2 사이만 채움 — 매주 새로움은 여기서만 발생.

> 강제 규칙(생성 프롬프트에 인라인): 캐릭터 외형은 캐논의 `ref_image_ids`만 사용(재생성 금지) · 기존 사실 수정 금지(추가만) · `taught_ledger`로 중복 출제 금지.

---

## 3. 커리큘럼 그라운드 트루스 — 시즌 1 (시드 콘텐츠로 코드화할 것)

세계관: **꼬꼬 마을**. 주인공 꼬꼬(호기심 많고 겁 많지만 성장하는 병아리), 친구 삐약이. 매주 꼬꼬가 *감정 사건*을 만나(정서) *나만의 방법으로 해결*(창의성)한다. 정서·창의성을 따로가 아니라 **한 이야기 안에** 엮는 것이 핵심.

| 주차 | 이야기(연속) | 정서 목표(CASEL→누리 사회관계) | 창의성 목표(Torrance→누리 예술경험) |
|---|---|---|---|
| 1 | 낯선 마을에 도착한 꼬꼬 | 내 감정 알아채기(자기인식): 기쁨·불안 | 나를 소개하는 나만의 방법(유창성) |
| 2 | 두근두근 알 깨기 대회 | 조급함 다스리기·기다리기(자기관리) | 알에서 뭐가 나올까 상상(유창성) |
| 3 | 삐약이가 울고 있어 | 친구 표정 읽고 공감(사회적 인식) | 위로하는 방법 여러 가지(융통성) |
| 4 | 비 오는 날의 발명 | 실망한 마음 다루기(자기관리) | 비를 막을 나만의 도구 만들기(독창성) |
| 5 | 마을 잔치 준비 대소동 | 다툼 풀고 나누기(관계 기술) | 잔치 아이디어 많이 내기(유창성) |
| 6 | 길 잃은 안개 숲 | 무서움 이겨내기(자기관리) | 길 찾는 여러 방법 떠올리기(융통성) |
| 7 | 꼬꼬의 비밀 선물 | 배려·고마움 전하기(관계/책임) | 나만의 선물 만들기(독창성) |
| 8 | 우리 마을 이야기책 | 두 마음 함께 느끼기(자기인식 종합) | 우리만의 이야기로 꾸미기(정교성) |

세션 흐름 예(3주차): 영상 A(삐약이가 운다 — 표정/공감) → 게임(표정 맞추기 + 정서표현 라운드) → 영상 B(어떻게 위로할까? 8초 대기 질문) → 클로저("나라면?" 창작 + 보상). 4주차는 이 서사를 이어받음.

> 메시징 가드레일(근거 D6/D7, 위반 시 리뷰 reject): SEL은 **"기술을 연습"** O / "기분이 좋아져요·정서조절" ❌ / 의학·DTx·ADHD ❌. 창의성은 "확산적 사고 *연습*", "영재성" 단정 ❌. 부모 리포트는 **관찰 가능한 활동량**만(내면 상태 단정 금지).

---

## 4. 코드 변경 지도 (재사용 / 신규 / 확장)

### 재사용 — 손대지 않음
- `supabase/migrations/0014_syllabus.sql`, `src/types/syllabus.ts` — 척추 그대로.
- `src/lib/game/engine.ts`, `src/data/games/asset-pools.ts`(emotion 풀 존재), `src/components/game/{SessionShell,PuzzleGame,EmotionExpressionGame,RewardMeta}.tsx`.
- `src/lib/episode-pipeline.ts`, `src/lib/video-providers/*` — 영상 파이프라인.
- RLS/인증 패턴(`0006`), `/api/inngest`.

### 확장 — 기존 파일에 추가 (리팩터 아님)
- `src/lib/game/curriculum-tags.ts` — CASEL 5 + Torrance 4 목표 코드 추가(`standard_source:'nuri'`, `interpretive:true`).
- `src/data/games/asset-pools.ts` — `emotion` 풀 보강 + `creativity` 풀 신규.
- `scripts/seed-syllabus.ts` — 정서·창의성 시드 함수 추가(기존 `korean_reading` 로직 보존).
- `src/lib/game/sel-report.ts` + `/dashboard/report` — 창의성 지표 추가.
- `src/inngest/functions/` + `/api/inngest` — 주간 cron 함수 등록(V1).

### 신규 — 추가 생성
- `supabase/migrations/0017_story_canon.sql` — `story_canon` 상태 + 차시-영상 다대일(아래).
- `src/data/syllabus-sel-emotion.ts`, `src/data/syllabus-creativity.ts` — 시즌 1 콘텐츠(§3).
- `src/lib/content/canon.ts` — 캐논 read/append + 연속성(모순·중복) 검증.
- `src/lib/content/topic-planner.ts` — 캐논+커서+engagement → 에피소드 브리프(LLM).
- `scripts/generate-weekly-episode.ts` — 주간 생성 batch(수동, Inngest 전 단계 동일 산출).
- `src/inngest/functions/weekly-episode-generator.ts` — 주간 cron(V1).

### 스키마 보강 결정 필요 (차시당 영상 2개)
현재 `syllabus_lessons.library_video_id` 는 단수다. 한 차시=영상 2편이므로 택1:
- (A) `syllabus_lessons.meta jsonb` 추가 — `{ "video_ids": [..], "segment_strategy": "..." }`. 최소 변경, 권장.
- (B) `lesson_videos(lesson_id, library_video_id, sort_order)` 조인 테이블 — 정규화, 확장성↑.
→ MVP는 (A). 0017에 포함. (결정은 `.ai/memory/decisions.md`에 기록.)

---

## 5. `story_canon` 상태 스키마(초안)

```jsonc
{
  "world": { "name": "꼬꼬 마을", "rules": ["알 깨기 대회가 있다", "..."] },
  "characters": [
    { "id": "kkokko", "name": "꼬꼬", "traits": ["호기심","겁많음→성장중"],
      "ref_image_ids": ["nano:kkokko_v1"], "arc_state": "S1 감정 어휘 배우는 중" }
  ],
  "plot_threads": [ { "id": "festival", "status": "준비중", "opened_week": 4 } ],
  "taught_ledger": [ { "objective": "sel_self_awareness_joy", "week": 1 } ],
  "recap_last": "지난 주 꼬꼬는 삐약이의 슬픔을 알아챘다.",
  "season": { "track": "sel_emotion", "code": "S1", "week_in_season": 3 }
}
```
- 카탈로그성 RLS(service_role만 쓰기, `0014`의 syllabus 카탈로그 패턴과 동일).
- `canon.ts`가 read→플래너→append를 트랜잭션처럼 다룬다(append-only, 기존 사실 수정 금지).

---

## 6. 주간 자동 생성 루프 (V1, Inngest cron)

```
[cron weekly]
 1 캐논 로드 + scope-and-sequence 다음 목표 결정
 2 토픽 플래너(LLM) → 에피소드 브리프 (정서 1 + 창의성 1)
 3 연속성 검증: taught_ledger 중복·캐릭터 모순 (실패 시 재프롬프트 1회)
 4 ClaudeDirector → EpisodeScript ×2 (영상 A/B, 각 ~3분)
 5 캐릭터 ref 캐시 재사용 → Seedance + Gemini TTS + lipsync (episode-pipeline)
 6 engine이 브리프 어휘·감정 풀에서 게임/퀴즈 라운드 구성 (LLM 0)
 7 published=false 로 library_videos + syllabus_lesson(준비중) 적재
 8 ── HUMAN QA 게이트 ── 안전·풀이가능성·연령 적합성 승인
 9 published=true 토글 + 캐논 append(recap/ledger/신규캐릭터) + 차시 해제
```
- 비용: 캐릭터 ref 1회 생성·영구 재사용 + **4~6주 버퍼 생성**(실시간 아님) + 영상은 세션의 ~6분뿐(나머지 LLM 0 게임). 실측 기준 15s $4.94 중 Seedance $4.54가 드라이버.
- 선행 인프라: `INNGEST_DEV=1`(`.env.local`) 해제 + Inngest Cloud 연결(`런칭_마스터플랜` 체크리스트 C). 그 전까지는 `generate-weekly-episode.ts` 수동 실행으로 동일 산출.

---

## 7. 작업 백로그 (`.ai/packages` 컨벤션, 의존 wave 순)

> 각 항목은 planner가 정식 패키지 스펙(Objective/Scope/Constraints/Deliverables/Validation/Dependencies/Handoff)으로 풀어 쓸 시드. 다음 마이그레이션 시퀀스 = **0017**.

**Wave 0 — 토대 (영상·LLM 불필요)**
- `db-story-canon` — `0017_story_canon.sql`: `story_canon` + `syllabus_lessons.meta jsonb`(차시 2영상). 카탈로그성 RLS.
- `lib-curriculum-tags-2track` — `curriculum-tags.ts`에 CASEL 5 + Torrance 4 코드 추가(기존 항목 보존).
- `data-asset-pools-2track` — `asset-pools.ts` emotion 보강 + creativity 풀 신규.

**Wave 1 — 시드 콘텐츠**
- `seed-syllabus-sel-emotion` — `src/data/syllabus-sel-emotion.ts`(시즌 1, §3) + `seed-syllabus.ts` 확장.
- `seed-syllabus-creativity` — `src/data/syllabus-creativity.ts`(시즌 1, §3) + `seed-syllabus.ts` 확장.

**Wave 2 — 연속성·생성 두뇌**
- `lib-canon` — `src/lib/content/canon.ts`: read/append + 연속성 검증.
- `lib-topic-planner` — `src/lib/content/topic-planner.ts`: 캐논+커서+engagement → 브리프(LLM).

**Wave 3 — 영상·게임 생성(수동 MVP)**
- `script-weekly-episode` — `episode-pipeline.ts`를 ~3분×2로 호출 + 캐릭터 ref 캐시 재사용.
- `ops-weekly-generate` — `scripts/generate-weekly-episode.ts`(batch, 수동). published=false.
- `game-creative-expression` — `EmotionExpressionGame`을 일반화한 창작형(참여도 점수).

**Wave 4 — 부모 가치**
- `parent-report-creativity` — `sel-report.ts`에 창의성 지표 + 대화 스타터.

**Wave 5 — 자동 스케줄(Inngest Cloud 후)**
- `infra-inngest-cloud` — `런칭_마스터플랜` 체크리스트 C.
- `inngest-weekly-generator` — `weekly-episode-generator.ts` cron.
- `feat-qa-gate` — 어드민 검토·발행 토글 + AI 사전 스크리닝.

---

## 8. `.ai/memory`에 추가할 항목 (integrator가 기록)

**decisions.md (append)**
- `2026-06-07 · 방향전환`: 콘텐츠 축을 과목→정서·창의성 2트랙 연속 서사로 전환. 이유: 학부모 결제 동기(인성·창의)·GACS-3 좌표 정합·2단계 예술 wedge로의 다리. 과목 시드/매트릭스는 legacy 보존.
- `2026-06-07 · 차시-영상`: 차시당 영상 2개는 `syllabus_lessons.meta jsonb`(A안)로. 이유: 최소 변경. 조인 테이블(B안)은 확장 필요 시 후속.

**invariants.md (append)**
- 콘텐츠 핫패스(게임·진도·퀴즈 출제)는 LLM 0 — LLM은 주간 생성(대본·플롯)에서만.
- 캐릭터 외형은 `story_canon.ref_image_ids`만 사용. 매 화 재생성 금지.
- `story_canon`은 append-only. 기존 사실 수정 금지(연속성 보장).
- 5~10세 대상 영상·게임은 휴먼 QA 게이트 없이 `published=true` 금지.
- 부모 대면 카피는 "기술 연습" 프레임. 효능·의학 클레임 금지(COPPA/기만광고).

**glossary.md (append)**
- 트랙(track): `sel_emotion` | `creativity` = `syllabuses.subject` 값.
- 스토리 캐논(story_canon): 세계관·캐릭터·플롯·학습 진도 상태. 연속 서사의 진실 원천.
- 토픽 플래너: 캐논+커서→주간 에피소드 브리프 생성기(`src/lib/content/topic-planner.ts`).
- 주차 에피소드: `syllabus_lesson` 1개 = 아이의 1회 ~20분 세션(영상 2 + 게임/퀴즈).

---

## 9. 지금 작업과의 충돌·주의

- **삭제 금지**: `syllabus-korean-preschool.ts`, `library-matrix.ts`, `library-matrix-90s.ts`, `generate-library-batch.ts`, `generate-library-episode-90s.ts` — 모두 legacy로 유지. 2트랙은 *추가*.
- **`library_videos` 스키마 재사용**: 연속 서사 영상도 같은 테이블. `topic` 컬럼에 트랙(`sel_emotion`/`creativity`)을 넣고, 회차·세계관 메타는 script/scene 또는 `meta`로.
- **AGENTS.md 준수**: 이 레포 Next.js는 표준과 다름 — 라우트·서버코드 변경 시 `node_modules/next/dist/docs/` 먼저 확인.
- **검증 정직성**: DB·시크릿 필요한 단계는 HUMAN 단계로 분리(기존 `영상후_게임퀴즈루프_구현계획.md` §8 관례).

---

## 부록: 한 줄 요약
**과목을 가르치던 독립 영상 제품을, 닭 마을에서 정서와 창의성을 한 이야기로 배우며 매주 다음 화가 자동으로 이어지는 제품으로 전환한다 — 척추·게임루프·영상 파이프라인은 그대로 두고, 그 위에 스토리 캐논과 주간 토픽 플래너를 얹는다.**
