# 꿈샘 데이터 측정 스펙 — 런칭용

> 작성일 2026-06-04 · 목적: 투자사 "정량 수치 부재" 피드백에 답하기 위해, **아산 꿈샘에서 실제 데이터를 측정**하는 실행 스펙. "지금 무엇을 가져오는가 → 무엇을 새로 수집하는가 → 무엇으로 증명하는가"를 한 문서로 확정한다.
> 연계: `KIOSK_KINDY_퍼널추적_설계.md`(퍼널/스키마 설계), `AI책놀이_데모_UIUX기획.md`(키오스크 데모 UX), `게임퀴즈_자체생성루프_PRD.md`(신규 신호원), `IR_보강안_2026-06.md`(투자사 대응)

---

## 0. 한 줄 요약

> **현재 우리는 "어떤 책이 인기 있나(책별 요청 수)" 1종만 안다. 런칭의 핵심은 꿈샘 키오스크를 계측해 "누가·얼마나 몰입했나·집으로 전환됐나"를 처음으로 측정하는 것이다.** 이 데이터가 없다는 사실이 곧 투자사 피드백의 원인이고, 이 데이터를 만드는 것이 곧 "왜 키오스크부터"의 답이다.

---

## 1. 현재 가져오는 데이터 (실측 인벤토리)

코드 정본 기준 — 북메이트(Smart-DID, 아산 꿈샘 운영 중)와 Kindy(eduvid)에서 **실제로 적재되는 것만** 정리한다.

### 1-A. 북메이트(Smart-DID) — 운영 중 / `library-did-main` Prisma·Postgres

| 테이블 | 실제 수집 필드 | 의미 | 한계 |
|---|---|---|---|
| `video_records` | `requestCount`(누적 요청), `lastRequestedAt`, `rankingScore` = requestCount + 최근7일×1.5, `status`, 책 메타(title/author/category…) | **책별 영상 수요 신호** | 책 단위 집계. 누가·몇 명·완주 여부 없음 |
| `shelf_maps` | bookId → 서가 위치(x,y) | 길찾기 | 행동 데이터 아님 |
| `recommendations` | 연령대별 추천 도서 | 큐레이션 | 정적 |
| `site_settings` | 운영 설정 | — | — |
| `audit.log`(파일) | `/auth` `/admin` `/internal` 호출만 (ts, ip, userId) | 관리자 감사 | **사용자(아동) 행동은 미기록** |
| ALPAS API(외부) | AE117 대출 베스트, AE113 이용자, AE108 대출이력 | 도서관 시스템 보유 | **조회만, 우리 DB에 미적재** |

**결론**: 운영 중인 키오스크가 실제로 남기는 행동 데이터는 사실상 `requestCount` 하나다. 영상 길이 24초(3×8초), 보관 90일, 책당 생성비 ≈ $0.80(trailer-engine 실측, QC 88점·생성 7분)는 알지만 — **사람·세션·완주·이탈·전환은 0.**

### 1-B. Kindy(eduvid) — 가정 내 구간 / Supabase·RLS

| 테이블 | 실제 수집 | 측정 가능 지표 |
|---|---|---|
| `view_events`(0002·0011) | child_id·video_id별 시청 이벤트(배치 PUT) | 가정 내 완주율·재시청 |
| `quiz_results` / `emoji_reactions` | 가정 내 퀴즈·이모지 | 학습 완료·정서 반응 |
| `children` | 아이 등록(첫 등록 +1 크레딧 트리거) | 활성 아이 수 |
| `purchases`(0004) | 일회성 크레딧 팩(single/pack6/pack15), provider 기본 `toss`(실제 카페24로 드리프트) | 결제 — **단, "구독/재결제" 개념 없음** |

**결론**: 가정 내 시청·퀴즈 인프라는 있으나 **(a) 익명 키오스크 구간이 분리 미연결**, **(b) 구독 모델 부재**로 퍼널 7·8단계(구독·재결제)를 못 잰다.

### 1-C. 핵심 갭 (투자사 정량 피드백 = 이 표)

지금 **측정 불가**한 것들:

- 키오스크 **세션/방문** 단위 로그 (몇 명이, 언제, 며칠 연속)
- 영상 **완주율** (position/duration 분포)
- 화면별 **이탈 퍼널** (S0→S5)
- **퀴즈·게임** 상호작용·정오답·재도전 (※ 애초에 키오스크에 퀴즈·게임 없음)
- 캐릭터·주제·기분 등 **취향 선택 신호**
- **QR 가정 전환** (스캔→가입→결제) 귀속
- **리텐션·구독·재결제**

> 이 갭이 곧 기회다. 경쟁사(학습지·키즈앱)도 못 가진 "도서관 현장 → 가정 전환"의 코호트 행동 데이터를, 우리가 **zero-CAC 채널에서 처음으로** 만든다.

---

## 2. 새로 수집할 데이터 (런칭과 함께 켜는 것)

`KIOSK_KINDY_퍼널추적_설계.md`의 스키마(`kiosk_sessions`/`kiosk_events`/`parent_attribution`)를 **꿈샘에서 실제 가동**한다. 신규 신호원으로 게임/퀴즈 루프(아래 §2-C)를 추가한다.

### 2-A. 익명 키오스크 구간 (PIPA 안전 · 식별정보 0)

`location_code = 'asan-kkumsaem'`. 무작위 세션 토큰 + 선택값·행동 이벤트만.

| 이벤트 | 페이로드 | 만드는 지표 |
|---|---|---|
| `demo_started` | — | 진입수(타일 노출 대비 진입율) |
| `step_select` | character / topic / mood | **취향 신호**(GACS-3 입력) |
| `video_started` · `video_progress`(5–10초 배치) · `video_completed`(ended 또는 ≥95%) | position_sec / duration_sec | **완주율 분포** |
| `quiz_started` · `quiz_completed`(score) · `quiz_skipped` | quiz_score | **퀴즈 완료율·정답률** |
| `game_started` · `game_round_completed` · `game_completed` | round, score, difficulty | **세션 체류시간·재도전율**(신규, §2-C) |
| `qr_shown` | — | QR 노출수 |

### 2-B. 익명→실명 다리 + 가정 내

- `qr_scanned`: `/start?ks=<token>` 도착 → 쿠키 `kindy_attr`(30–90일).
- `parent_attribution`: 가입 시 1행(first-touch) → 익명 세션과 부모 계정 연결.
- 이후 `view_events`·`purchases`는 `parent_id` 조인으로 키오스크 세션까지 자연 추적.

### 2-C. 신규 신호원 — 게임/퀴즈 자체생성 루프

20분 세션을 영상 대신 자체생성 게임+퀴즈로 채우면(상세: `게임퀴즈_자체생성루프_PRD.md`), **영상 1편 = 신호 수십 개**에서 **세션 1회 = 신호 수백 개**로 데이터 밀도가 한 자릿수 올라간다. 게임 라운드마다 난이도 적응(정답률→다음 난이도)이 곧 GACS-3 학습 신호가 된다.

| 게임 신호 | 측정 의미 |
|---|---|
| 라운드별 정답률·소요시간 | 학습 효과·집중도 곡선 |
| 자발적 재도전 | 흥미·몰입(이탈 반대) |
| 난이도 적응 궤적 | 개인화가 실제로 fit 되는지 |
| 세션 총 체류(20분 목표 대비) | 키오스크 lock-in 강도 |

---

## 3. 측정 지표 (투자사에게 보여줄 숫자)

퍼널 단계별 전환율을 **꿈샘 단일 코호트**로 산출. 정의는 설계 문서 §8(B안: 구독 테이블 없이 purchases로 정의) 기준, 단 §5의 구독 모델(A안)은 Pre-A 전 도입 권장.

| 지표 | 정의 | Why it matters |
|---|---|---|
| 진입율 | demo_started / 타일 노출 | 키오스크가 트래픽을 만드나 |
| **완주율** | video_completed / video_started | 콘텐츠가 아이를 잡나(핵심) |
| 퀴즈 완료율 | quiz_completed / video_completed | 학습 프레이밍이 작동하나 |
| 게임 재도전율 | 재도전 세션 / 게임 세션 | 흥미 지속(20분 lock-in) |
| **QR 스캔율** | qr_scanned 세션 / qr_shown 세션 | **현장→가정 전환**(이 사업의 심장) |
| 가입 전환율 | attribution 행 / qr_scanned | 무료 유입 품질 |
| **유료 전환율** | 첫 paid 보유 parent / 가입 | LTV/CAC 검증(IR 가정 vs 실측) |
| 재결제율 | paid 2건+ / paid 1건+ | 리텐션·구독 가능성 |
| N일 리텐션 | 가입 후 N일 내 view/생성 발생 | 집에서 계속 쓰나 |

> **목표는 "예쁜 숫자"가 아니라 "IR 가정의 실측 검증"이다.** IR은 ARPU 25,000 / LTV 150,000(6개월) / CAC 0(도서관)·30,000(paid IG) / LTV·CAC 3–5×(도서관 코호트)를 *가정*으로 적었다. 꿈샘 측정의 임무는 이 가정에 **실측 오차범위**를 붙이는 것이다.

---

## 4. 대시보드 (Supabase + 자체 `/admin`)

- 분석 뷰: `funnel_daily`(일자별 단계 카운트), `kiosk_session_outcomes`(세션→스캔→가입→결제 플래그), `attribution_revenue`(location별 매출 귀속).
- 구현: Supabase SQL 뷰 + Kindy `/admin` 라우트 Recharts(단계 막대 + 전환율 + location 필터). 권한: 관리자 전용.
- 투자자용 1페이지: "꿈샘 코호트 — 진입 N · 완주율 X% · QR 스캔율 Y% · 가입 Z명 · 유료 W명" 자동 갱신.

---

## 5. 구현 순서 (위험·의존성 기준)

영상 생성 파이프라인(Inngest)·DNS 차단과 **무관하게** 착수 가능 — 키오스크 데모는 사전 제작 샘플 영상으로 동작하므로 측정부터 켤 수 있다.

1. **마이그레이션** `00xx_kiosk_funnel.sql` — kiosk_sessions / kiosk_events / parent_attribution + 익명 insert RLS.
2. **익명 인제스트 API** `POST /api/kiosk/events`(세션 생성·rate limit·service-role 쓰기).
3. **키오스크 데모 실동화**(`demo/kiosk`) — 샘플 영상 재생+완주 이벤트 / 퀴즈·게임 완료 이벤트 / **실제 QR**(`qrcode` 패키지, 현재 FakeQR 교체).
4. **`/start?ks=`** 라우트 — qr_scanned 적재 + 쿠키.
5. **가입 attribution 훅** — auth 콜백/온보딩 첫 진입에서 parent_attribution 1회 기록.
6. **결제 단계 정의** — 즉시 측정은 B안, 진짜 구독은 A안(`subscriptions` 테이블) — Pre-A 트리거(유료 1K·LTV/CAC≥3×) 전 도입.
7. **분석 뷰 + `/admin` 대시보드**.
8. **게임/퀴즈 루프** 이벤트 연동(§2-C, PRD 일정과 합류).

### 차단요소(병행 해소)
- Inngest 프로덕션 미연결(`INNGEST_DEV=1`) → 실영상 생성 차단(측정엔 무관).
- kindy.kr DNS(카페24 묶임) → Cloudflare NS 이관 추천. QR 목적지는 임시 LB URL(`34.8.67.108`)로 우회 가능.
- 사업자등록·본인인증·약관 — 유료 전환 측정 전 마감 필요(Footer placeholder 제거).

---

## 6. PIPA / 아동 안전

- 키오스크 익명 구간: 식별정보 미수집(무작위 토큰 + 선택·행동만). 카메라·이름·연락처 없음.
- QR·쿠키는 기기 단위 attribution, 개인 식별 아님. 동의는 가입 시점(집)으로 미룸.
- 화면 하단 고지: "이 놀이는 개인정보를 저장하지 않아요." `location_code` 외 위치정보 미수집.

---

## 부록: 출처(코드 정본)
- 북메이트: `lib0319/Smart-DID/library-did-main/packages/backend/prisma/schema.prisma`(이벤트 모델 없음), `repositories/video.repository.ts:91`(requestCount 증가), `plugins/audit.ts`(admin/auth/internal만), `꿈샘/01_QUICK_REFERENCE.md`(24초·90일·랭킹식·책당 $0.80).
- Kindy: `KIOSK_KINDY_퍼널추적_설계.md` §4·§8, `STATUS.md`(비용 실측 $4.94/15s, 차단요소), `supabase/migrations/0002·0004·0011`.
